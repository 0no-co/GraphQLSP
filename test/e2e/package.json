{
  "private": true,
  "name": "e2e-tests",
  "scripts": {
    "prepare-build": "cd ../.. && pnpm build",
    "pretest": "pnpm prepare-build",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "vitest": "^0.30.1",
    "typescript": "^5.0.4"
  }
}
