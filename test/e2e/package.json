{
  "private": true,
  "scripts": {
    "prepare-build": "cd ../.. && pnpm build",
    "prepare-tests": "cd fixture-project && pnpm install",
    "postinstall": "pnpm prepare-build && pnpm prepare-tests",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "vitest": "^0.30.1",
    "typescript": "^5.0.4",
    "@types/node": "18.15.11"
  }
}
